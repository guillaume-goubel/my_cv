<div class="container">
    
    {% if app.request.getLocale() == 'en' %}
        <div>{{project.descriptionEn|raw }}</div>
    {% else %}
        <div>{{project.descriptionFr|raw }}</div>
    {% endif %}

</div>
